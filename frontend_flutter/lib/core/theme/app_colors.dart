import 'package:flutter/material.dart';

/// Paleta premium rediseñada para APYRA ADMIN
/// Estilo SaaS moderno, ejecutivo y profesional
class AppColors {
  AppColors._();

  // ── Primarios ──
  static const Color primary = Color(0xFF1A56DB);
  static const Color primaryLight = Color(0xFFEFF6FF);
  static const Color primaryDark = Color(0xFF143FAF);
  static const Color primarySoft = Color(0xFFF0F4FA);

  // ── Fondos ──
  static const Color background = Color(0xFFF0F4FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);
  static const Color surfaceElevated = Color(0xFFFAFBFC);

  // ── Texto ──
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);

  // ── Bordes ──
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);
  static const Color divider = Color(0xFFF1F5F9);

  // ── Semánticos ──
  static const Color success = Color(0xFF059669);
  static const Color successLight = Color(0xFFF0FDF4);
  static const Color warning = Color(0xFFD97706);
  static const Color warningLight = Color(0xFFFFFBEB);
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEF2F2);
  static const Color info = Color(0xFF0891B2);
  static const Color infoLight = Color(0xFFECFEFF);

  // ── Sidebar ──
  static const Color sidebarBg = Color(0xFF0F172A);
  static const Color sidebarSurface = Color(0xFF1E293B);
  static const Color sidebarText = Color(0xFF94A3B8);
  static const Color sidebarActiveText = Color(0xFFFFFFFF);
  static const Color sidebarHover = Color(0xFF1E293B);
  static const Color sidebarActive = Color(0xFF1A56DB);
  static const Color sidebarBorder = Color(0xFF1E293B);
  static const Color sidebarIcon = Color(0xFF64748B);
  static const Color sidebarIconActive = Color(0xFFFFFFFF);

  // ── Sombras ──
  static const Color shadowColor = Color(0x0A1A56DB);
  static const Color shadowSm = Color(0x08000000);
  static const Color shadowMd = Color(0x0F000000);
}
